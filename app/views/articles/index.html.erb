<div id="player"></div>




<p id="notice"><%= notice %></p>

<h1>Articles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>


<script>
  var player = new Clappr.Player({  source: 'http://clappr.io/highline.mp4', parentId: "#player"});
  // var player = new Clappr.Player({source: "https://www.youtube.com/watch?v=_jHpnb-QmTA", parentId: "#player"});
  // var player = new Clappr.Player({source: "https://video.tcs.com/uploads/q3-fy-20.mp4", parentId: "#player"});
  // var player = new Clappr.Player({source: "https://video.tcs.com/hlslive/live1/livestream1/index.m3u8", parentId: "#player"});

  // var player = new Clappr.Player({
  // source: 'https://video.tcs.com/hlslive/live1/livestream1/index.m3u8',
  // parentId: '#player',
  // flushLiveURLCache: false,
  // autoPlay: true
  // //poster: 'https://video.tcs.com/assets/images/logo.jpeg'
  // //html: '<p>This big overlay is a content plugin</p>',
  // //style: {p: {fontSize: 40}}
  // });
</script>